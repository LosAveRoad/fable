apiVersion: v1
kind: Secret
metadata:
  name: fable-secret
  namespace: fable
type: Opaque
stringData:
  FABLE_MYSQL_USER: fable
  FABLE_MYSQL_PASSWORD: replace-me
  FABLE_MYSQL_DATABASE: mychat
  FABLE_REDIS_PASSWORD: replace-me
  FABLE_JWT_SECRET: replace-with-a-random-secret
