# Fable IM 分层压测报告

## 测试边界

目标 `api.fableim.lol` 在压测容器内固定映射到 `38.147.170.246`，避免本地 DNS 代理影响结果。测试生成器不运行在 VPS1/VPS2。

## 结果记录

| 阶段 | 路径/场景 | 目标负载 | 实际负载 | 错误率 | p95 | p99 | 首个瓶颈证据 |
|---|---|---:|---:|---:|---:|---:|---|
| 1 | HTTPS /healthz | | | | | | |
| 2 | NodePort /healthz | | | | | | |
| 3 | 单 Pod / 双 Pod | | | | | | |
| 4 | 业务 API | | | | | | |
| 5 | WebSocket | | | | | | |
| 6 | 群聊广播 | | | | | | |

## 组件指标

填写 `collect-vps2.sh` 和 `collect-vps1.sh` 生成的归档结果，关联每一轮测试时间窗口。

## 判定规则

稳定容量定义为错误率 <1%、p99 <2s、无 Pod 重启、无持续 Kafka lag、无数据库连接池耗尽。超过任一条件即停止升压，复测确认后记录为瓶颈点。
